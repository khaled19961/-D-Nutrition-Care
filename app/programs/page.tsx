import Link from "next/link";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { SiteChrome } from "@/components/site-chrome";
export const revalidate=60;
export default async function ProgramsPage(){const s=await createSupabaseServerClient();const {data,error}=await s.from("nutrition_programs").select("id,title_ar,description_ar,duration_days").eq("status","published").order("created_at",{ascending:false});return <SiteChrome><main><section className="page-hero"><div className="container"><span>البرامج</span><h1>برامج غذائية للمتابعة المنظمة</h1><p>استعرض البرامج المنشورة فعلياً في المنصة قبل البدء.</p></div></section><section><div className="container"><div className="services-grid">{error?<div className="empty-state">تعذر تحميل البرامج حالياً.</div>:data?.length?data.map(p=><article className="service-card" key={p.id}><h3>{p.title_ar}</h3><p>{p.description_ar||"برنامج غذائي منشور."}</p><div className="program-meta"><span>{p.duration_days?String(p.duration_days)+" يوم":""}</span><Link href="/booking">ابدأ الحجز ←</Link></div></article>):<div className="empty-state">لا توجد برامج منشورة حالياً.</div>}</div></div></section></main></SiteChrome>}